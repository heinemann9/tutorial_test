# vercel-supabase-tutorial (test1)

`Vercel 배포 따라하기` 스크린샷 캡처용 테스트 저장소. `NEXT_PUBLIC_TEAM_NAME` 환경변수 값을 화면에 그대로 보여주는 최소 데모입니다.

- 환경변수 없음 → "우리 팀 사이트" / "아직 설정 안 됨"
- `NEXT_PUBLIC_TEAM_NAME=1조` 설정 후 재배포 → "1조 사이트" / "환경변수 적용됨"
